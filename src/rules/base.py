"""Base rule definitions (placeholder)."""


class Rule:
    """Base rule class placeholder."""

    def __init__(self, rule_id: str, description: str) -> None:
        self.rule_id = rule_id
        self.description = description
